# Convert to absolute direction of bias or indirectness

Converts qualitative bias direction labels (e.g. "Favours comparator")
into absolute directions ("left" or "right") based on effect size and
bias type.

## Usage

``` r
tri_absolute_direction(dat)
```

## Arguments

- dat:

  Data in long format
